<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Preset fonts are downloaded over HTTPS from GitHub Releases. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      mermaid 图表「保存图片」需要写公共相册。
      API 29 起走 MediaStore 分区存储，不需要这个权限；只有 API 26-28 才要，
      所以用 maxSdkVersion 限住，新版系统上不会申请任何存储权限。
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 禁用备份功能 -->
    <application
        android:name=".JasmineApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MyApplication">
        <!--
          Opts AppCompat into persisting in-app language picks made via
          AppCompatDelegate.setApplicationLocales() across process restarts on
          API 24-32. Without this service + metadata the auto-store check finds
          no AppLocalesMetadataHolderService and silently skips persistence, so
          the chosen language reverts to "follow system" after the app is
          killed. (On API 33+ the framework LocaleManager persists per-app
          locales system-side.)
        -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
